function promedio(a: number, b: number): number{
    return (a+b)/2
}

const cuadrado = (a: number): number => a**2;



const raizCuadrada = (a: number): number => a**(1/2);

//Exports cuadrado, raizCuadrada
export {promedio, cuadrado, raizCuadrada}